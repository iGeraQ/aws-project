services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: aws-project-app:latest
    ports:
      - "8081:8080"
    env_file:
      - .env
    environment:
      - SPRING_PROFILES_ACTIVE=jpa
    restart: always
